---
up:
  name: httpbin_check
  request:
    method: GET
    url: "{{.api_base}}/200"
    headers:
      - name: Authorization
        value: "Basic {{._auth_token}}"
  response:
    result_code: [ "200" ]
