---
up:
  name: check-a-with-a1
  request:
    method: GET
    url: "{{.env.api_base}}/a"
    headers:
      - name: Authorization
        value: "Basic {{.auth.a1}}"
  response:
    result_code: [ "200" ]
