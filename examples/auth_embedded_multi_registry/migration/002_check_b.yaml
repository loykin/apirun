---
up:
  name: check-b-with-a2
  request:
    method: GET
    url: "{{.env.api_base}}/b"
    headers:
      - name: Authorization
        value: "Basic {{.auth.a2}}"
  response:
    result_code: [ "200" ]
