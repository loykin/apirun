---
migrate_dir: ./migration

env:
  api_base: http://127.0.0.1:18080

store:
  type: sqlite
  sqlite:
    path: ./validation_state.db
  save_response_body: false

logging:
  level: info
  format: text