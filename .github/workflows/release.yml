name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build binaries
    runs-on: ubuntu-latest
    env:
      CGO_ENABLED: 0
    strategy:
      fail-fast: false
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c
        with:
          go-version-file: go.mod
          cache: true

      - name: Show Go version
        run: go version

      - name: Build
        shell: bash
        run: |
          mkdir -p dist
          BIN_NAME=apirun
          EXT=""
          if [ "${{ matrix.goos }}" = "windows" ]; then EXT=".exe"; fi
          BIN_FILE="${BIN_NAME}${EXT}"
          OUT_DIR="${BIN_NAME}_${{ matrix.goos }}_${{ matrix.goarch }}"

          echo "Building $BIN_FILE for $OUT_DIR"
          GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -o "$BIN_FILE" ./cmd/apirun

          mkdir -p "$OUT_DIR/bin"
          cp "$BIN_FILE" "$OUT_DIR/bin/"
          [ -f README.md ] && cp README.md "$OUT_DIR/"
          [ -f LICENSE ] && cp LICENSE "$OUT_DIR/"
          if [ -d config ]; then
            cp -R config "$OUT_DIR/"
            find "$OUT_DIR/config" -name '*.db' -type f -delete || true
          fi

          if [ "${{ matrix.goos }}" = "windows" ]; then
            zip -9 -r "dist/${OUT_DIR}.zip" "$OUT_DIR"
          else
            tar -czf "dist/${OUT_DIR}.tar.gz" "$OUT_DIR"
          fi

          rm -rf "$OUT_DIR" "$BIN_FILE"


      - name: Upload build artifacts
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f
        with:
          name: binaries-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/*
          if-no-files-found: error

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build

    permissions:
      contents: write
      id-token: write   # 🔑 cosign keyless signing / provenance requires this

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          pattern: binaries-*
          merge-multiple: true

      - name: Display files to release
        run: ls -la

      - name: Generate checksums
        run: |
          sha256sum *.tar.gz *.zip > checksums.txt
          cat checksums.txt

      - name: Install cosign
        uses: sigstore/cosign-installer@v3

      - name: Sign artifacts with cosign
        run: |
          for f in *.tar.gz *.zip checksums.txt; do
          cosign sign-blob --yes $f \
            --output-signature $f.sig \
            --output-certificate $f.pem
          done
        env:
          COSIGN_EXPERIMENTAL: "true"

      - name: Generate provenance
        run: |
          for f in *.tar.gz *.zip; do
          cosign attest-blob --yes $f \
            --type slsaprovenance \
            --predicate <(echo '{
              "buildType": "https://slsa.dev/container/v1",
              "builder": { "id": "https://github.com/loykin/apirun" },
              "invocation": { "configSource": { "uri": "git+https://github.com/loykin/apirun@${GITHUB_REF_NAME}" } }
            }') \
            --output-attestation $f.intoto.jsonl \
            --output-certificate $f.pem
          done
        env:
          COSIGN_EXPERIMENTAL: "true"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda
        with:
          files: |
            *.tar.gz
            *.zip
            checksums.txt
            *.sig
            *.intoto.jsonl
          generate_release_notes: true
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
